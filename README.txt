Progress:
-print out usage messages and error: ok for default, not same as testcases
-in file_type: 
	+using os.listdir()=> assume that just checking the current the directory, need to clarify with Prof if we need to check any path

- draw structure of canvas: restart button, grid, and label score 
