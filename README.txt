Progress:
-print out usage messages and error
-in file_type: 
	+using os.listdir()=> assume that just checking the current the directory, need to clarify with Prof if we need to check any path
	+also, the error message matches with slide, not with the given test cases


